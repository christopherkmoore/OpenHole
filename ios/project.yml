name: OpenHole
options:
  bundleIdPrefix: com.openhole.ai
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

packages:
  Citadel:
    url: https://github.com/orlandos-nl/Citadel
    from: "0.12.0"
  swift-markdown-ui:
    url: https://github.com/gonzalezreal/swift-markdown-ui
    from: "2.4.0"
  WireGuardKit:
    path: Packages/WireGuardApple

targets:
  OpenHole:
    type: application
    platform: iOS
    sources:
      - OpenHole
    configFiles:
      Debug: Local.xcconfig
      Release: Local.xcconfig
    info:
      path: OpenHole/Resources/Info.plist
      properties:
        CFBundleDisplayName: OpenHole
        LSRequiresIPhoneOS: true
        NSMicrophoneUsageDescription: "OpenHole uses the microphone for voice input to send prompts to Claude."
        NSSpeechRecognitionUsageDescription: "OpenHole uses speech recognition to transcribe voice input into text prompts."
        NSLocalNetworkUsageDescription: "OpenHole connects to your server over the local network via SSH."
        NSBonjourServices:
          - _ssh._tcp
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.openhole.ai
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: "5.10"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    dependencies:
      - sdk: AppIntents.framework
      - package: Citadel
      - package: swift-markdown-ui
        product: MarkdownUI
      - target: OpenHoleTunnel
        embed: true
    entitlements:
      path: OpenHole/Resources/OpenHole.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.network.client: true
        com.apple.security.device.microphone: true
        com.apple.developer.networking.networkextension:
          - packet-tunnel-provider
        com.apple.security.application-groups:
          - group.com.openhole.ai

  OpenHoleTunnel:
    type: app-extension
    platform: iOS
    sources:
      - OpenHoleTunnel
    configFiles:
      Debug: Local.xcconfig
      Release: Local.xcconfig
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.openhole.ai.tunnel
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: "5.10"
        LIBRARY_SEARCH_PATHS: "$(SRCROOT)/Packages/WireGuardApple/Sources/WireGuardKitGo/out"
    dependencies:
      - package: WireGuardKit
        product: WireGuardKit
    entitlements:
      path: OpenHoleTunnel/OpenHoleTunnel.entitlements
      properties:
        com.apple.developer.networking.networkextension:
          - packet-tunnel-provider
        com.apple.security.application-groups:
          - group.com.openhole.ai
    info:
      path: OpenHoleTunnel/Info.plist
      properties:
        NSExtension:
          NSExtensionPointIdentifier: com.apple.networkextension.packet-tunnel
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).PacketTunnelProvider
        CFBundlePackageType: "XPC!"
        CFBundleDisplayName: OpenHoleTunnel
